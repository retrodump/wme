Game.Log("writing to log");

var something = "some value";

while(true)
{
  Sleep(5000);

  for (var i = 0; i < 100; i++)
  {
    this.CustomMethod("param" + ToString(i));
    this.CustomProp = "custom" + i;
    Sleep();
  }
}


method CustomMethod(param)
{
  var local = "local";
  //Game.Msg("in method: " + param);
}
